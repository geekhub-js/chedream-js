angular.module('profile.routing', [
    'profile.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('profile', {
                
            });
    });