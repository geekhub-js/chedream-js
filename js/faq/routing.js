angular.module('faq.routing', [
    'faq.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('faq', {
                
            });
    });