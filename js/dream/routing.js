angular.module('dream.routing', [
    'dream.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('dream', {
                
            });
    });