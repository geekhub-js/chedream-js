angular.module('allDreams.routing', [
    'allDreams.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('allDreams', {
                
            });
    });