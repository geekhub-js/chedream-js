angular.module('error.routing', [
    'error.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('error', {
                
            });
    });