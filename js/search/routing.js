angular.module('search.routing', [
    'search.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('search', {
                
            });
    });