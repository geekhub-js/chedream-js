angular.module('popUps.routing', [
    'popUps.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('popUps', {
                
            });
    });