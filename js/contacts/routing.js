angular.module('contacts.routing', [
    'contacts.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('contacts', {
                
            });
    });