angular.module('oldBrowser.routing', [
    'oldBrowser.controller',
    'ui.router'
])
    .config(function ($stateProvider) {
        $stateProvider
            .state('oldBrowser', {
                
            });
    });